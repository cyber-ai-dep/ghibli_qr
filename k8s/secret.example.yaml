# Template only — copy to secret.yaml, fill in the real value, and apply.
# Do NOT commit the filled-in secret.yaml to version control.
apiVersion: v1
kind: Secret
metadata:
  name: ghibli-api-secret
type: Opaque
stringData:
  ARK_API_KEY: "<your-ark-api-key>"
